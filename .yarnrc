# Defense-in-depth against npm supply-chain attacks: refuse to run install
# lifecycle scripts. Override per-invocation with `--no-default-rc` or by
# editing this file if a legitimate package requires a build step.
ignore-scripts true
